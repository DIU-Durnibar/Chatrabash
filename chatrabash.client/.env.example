# Local dev (default in api.js if unset). For Vercel production use same-origin + vercel.json proxy.
VITE_API_BASE_URL=http://localhost:5091

# Vercel production (optional explicit):
# VITE_API_BASE_URL=same-origin
